#include <stdio.h>
int main() {
	int i = 2;
	
	printf("help");
	int list[500000];
	printf("%d", list[100]);
	if ((list[100] != 1) && (list[100] != 1)) {
		printf("this works");
	}
	for (int i; i <= 10000000; i++) {
		int num = i;

	}
	return 0;
}